import React from 'react'; 

const ProgressBar = ({value, max}) => {
    return (
        <progress value={value}
    )
}

export default ProgressBar; 

//need to import ProgressBar into BookInfo
//may need to remove {attempt} from BookInfo
//need to call Filler from BookInfo?

